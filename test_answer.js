const fetch = require('node-fetch');
// Assuming backend is on localhost:3003
async function test() {
    // 1. First get an active interview from the db or create one
    // But since I don't have the token easily, I will just call the backend method directly.
}
test();
